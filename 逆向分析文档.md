# 中华珍宝馆（g2.ltfc.net）高清图下载逆向分析文档

## 1. 目标

从作品页下载**高清原图**（非缩略图）：

```
https://g2.ltfc.net/view/SUFA/67dd6df16c96742943b9bad8?originType=SUFA_PAGE
```

该作品为《周铜祭器十事拓本》，实际包含 **10 页**（第 1 页 ~ 第 10 页），
每页是一张约 **10500 × 23500 像素**（约 2.5 亿像素）的高清拓片图。

最终产物：`download_ltfc.py`，用 `requests` 下载瓦片并用 PIL 拼接成完整高清图。

---

## 2. 逆向环境与工具

| 工具 | 用途 |
|------|------|
| `curl` | 抓取页面 HTML、JS 文件 |
| `camoufox` 反检测浏览器（MCP） | 加载页面、抓取网络请求、观察真实瓦片请求 |
| Python + `requests` | 最终下载脚本 |
| Python `re` / `hashlib` | 解析数据、验证 MD5 |

---

## 3. 逆向过程

### Phase 0 —— 观察页面

用 `curl` 抓取页面，返回 `200`，是一个 **Next.js** 服务端渲染（SSR）应用：

```http
X-Powered-By: Next.js
Vary: RSC, Next-Router-State-Tree, Next-Router-Prefetch, Accept-Encoding
```

关键点：作品与图片的**元数据直接内嵌在 HTML** 的 RSC payload 中
（`self.__next_f.push([...])`，JSON 以转义形式 `\"` 存储）。

---

### Phase 1 —— 分析 SSR 数据，拿到图片列表

从 HTML 中提取出图片相关字段，发现数据为：

```json
{
  "name": "第1页",
  "resourceId": "6228ab5acbfdb8048a47dd17",
  "maxlevel": 18,
  "minlevel": 12,
  "size": { "width": 10385, "height": 23546 },
  "tilesSource": "TILES_SOURCE_ALIYUN",
  "tilesDir": "cagstore",
  "thumbTileUrl": "http://cag-tiles.oss-cn-beijing.aliyuncs.com/cagstore/6228ab5acbfdb8048a47dd17/13/0_0.jpg?OSSAccessKeyId=...&Expires=...&Signature=..."
}
```

结论：
- 每页图片由一个 `resourceId` 标识（`6228ab5acbfdb8048a47dd17` ~ `...d20`，共 10 个）。
- 图片以**金字塔瓦片**存储，级别 `minlevel=12` ~ `maxlevel=18`。
- `thumbTileUrl` 是 level 13 的**缩略图**，用的是**阿里云 OSS 签名 URL**（`cag-tiles.oss-cn-beijing.aliyuncs.com`，带 `OSSAccessKeyId` / `Expires` / `Signature`）。

---

### Phase 2 —— 直接访问瓦片失败（发现鉴权）

尝试直接请求瓦片（不带签名参数）：

```
https://cag-tiles.oss-cn-beijing.aliyuncs.com/cagstore/6228ab5acbfdb8048a47dd17/13/0_0.jpg
```

返回 **403**：

```xml
<Error><Code>AccessDenied</Code>
<Message>You have no right to access this object because of bucket acl.</Message></Error>
```

结论：OSS 桶是**私有**的，必须带签名。而 OSS 的 `Signature` 是用
`AccessKeySecret` 对「对象路径」算 HMAC 得到的，**无法从客户端伪造**。
所以需要另找出路。

---

### Phase 3 —— 浏览器抓包，发现真实瓦片请求

用 camoufox 浏览器加载页面并抓取网络，发现浏览器**并不是**请求 OSS 域名，
而是请求另一个 CDN 域名 `cag-ac.ltfc.net`，鉴权参数是 `auth_key`：

```
https://cag-ac.ltfc.net/cagstore/6228ab5acbfdb8048a47dd1d/13/0_0.jpg?auth_key=1787062752-0-0-63409cfdc85493e8f823ef07ad259b4c
```

观察到的关键现象：

1. **不同级别有独立瓦片文件**（`/12/`、`/13/`、`/14/` ...），无需 `image_process`。
2. `auth_key` 的格式是 `{expires}-0-0-{32位hex}`（形如阿里云 CDN Type-A 鉴权）。
3. `expires` **每次加载都会变化**（第一次 `1787062544`，第二次 `1787062752`），
   说明 `auth_key` 是**客户端实时生成**的。
4. 网格规模随级别翻倍：
   - level 12：1×1
   - level 13：1×2
   - level 14：2×3
   - level 18：21×46（每页约 966 片）

---

### Phase 4 —— 排除"服务端下发"，确认"客户端生成"

页面加载时打到的 API 只有 6 个（`api.quanku.art`），逐个抓取响应体：

| 接口 | 响应 |
|------|------|
| `TouristDeviceService/registerDevice` | `{"code":"SUCCESS",...}` |
| `TouristService/getDeviceOrgIp` | 设备 IP |
| `TouristService/enter` | `{"code":"SUCCESS","touched":-1}` |
| `TouristDeviceService/getDevice` | 设备信息 |
| `HDPicService/listHDPSlice` | `{"data":[],"total":0}`（注释切片，与瓦片无关） |
| `BaseService/getWebConfig` | 只有语音列表 |

**没有一个接口返回 `auth_key` 或瓦片 URL** → 排除"服务端下发签名"的可能。

又通过 `document.querySelectorAll('iframe')` 确认**页面无 iframe**，
viewer 是 `<canvas>` 渲染 → 瓦片 URL 一定在页面主进程的某个 JS chunk 里生成。

---

### Phase 5 —— 定位前端 viewer 的 JS 代码

#### 5.1 找到 viewer 入口

页面 chunk（`app/(viewer)/view/[src]/[id]/page-e47ab76cebf705d8.js`）里：

```js
let o = loadable(() => Promise.all([
  n.e(6523), n.e(6990), n.e(7809), n.e(7327), n.e(9761), n.e(9061),
  n.e(6326), n.e(3145), n.e(4506), n.e(7155), n.e(7204), n.e(9519),
  n.e(9337), n.e(2957), n.e(4241), n.e(1434), n.e(9636), n.e(3464),
  n.e(3488), n.e(2376), n.e(8807), n.e(3466), n.e(8686), n.e(5610),
  n.e(1253)
]).then(n.bind(n, 47234)), ...);
```

真正的看图器是 **webpack 模块 47234**，它依赖上面 25 个 chunk。

#### 5.2 拿到 chunk ID → 文件名映射

webpack 运行时（`webpack-9df6862f0e97d6ab.js`）里的 `l.u` 函数保存了映射，例如：

```js
l.u = function(e) {
  return 5610 === e ? "static/chunks/5610-6ec0727a379f5842.js"
       : 8807 === e ? "static/chunks/8807-c3a75832eae93e10.js"
       : 9636 === e ? "static/chunks/9636-1234f39ab007dcc5.js"
       : 2376 === e ? "static/chunks/2376-89c439eeb8c36ebe.js"
       : "static/chunks/" + (({9761:"ed0e4a1e"})[e]||e) + "." + ({...hash映射...})[e] + ".js";
}
```

据此下载相关 chunk 并搜索关键字。

---

### Phase 6 —— 还原鉴权算法（核心）

在 `chunk 5610-6ec0727a379f5842.js` 的模块 `26549` 中找到了**完整算法**：

```js
function s(e, t) {
  return "TILES_SOURCE_ALIYUN" === t
    ? function (e) {
        if (!e) return e;
        let t = e.match(/^(https*:\/\/[\w-.]*)(\/.+\.(jpeg|jpg|png))\?*(.*)$/);
        if (!t) return e;
        let n = t[1],              // 域名   https://cag-ac.ltfc.net
            o = t[2],              // 路径   /cagstore/{resourceId}/{level}/{x}_{y}.jpg
            s = t[4] || "";        // 已有 query
        s && (s = s + "&");
        let a = Math.floor((Date.now() + 0) / 1e3);   // expires = 当前秒级时间戳
        let l = o + "-" + a + "-" + 0 + "-" + 0 + "-" + "ltfcdotnet";  // 待哈希字符串
        let c = md5(l);            // MD5（小写 hex）
        return n + o + "?" + s + "auth_key=" + a + "-" + 0 + "-" + 0 + "-" + c;
      }("https://cag-ac.ltfc.net" + (e || ""))
    : /* 另一套 cag.ltfc.net 的 sign 算法，本项目未用到 */;
}
```

**提炼出的最终鉴权算法**：

```
expires  = 当前 Unix 时间戳（秒）
path     = /cagstore/{resourceId}/{level}/{x}_{y}.jpg
sign_str = "{path}-{expires}-0-0-ltfcdotnet"
auth_key = MD5(sign_str)
URL      = https://cag-ac.ltfc.net{path}?auth_key={expires}-0-0-{auth_key}
```

> **密钥就是 `ltfcdotnet`**，硬编码在前端 JS 中。

同时确认：
- MD5 库在 `chunk 9636-1234f39ab007dcc5.js`（`js-md5`）。
- 瓦片路径拼接在 `chunk 8807-c3a75832eae93e10.js` 的 `createHDPTileSource`：

  ```js
  // e=resourceId, n=level, o=列x, r=行y
  "/" + (tilesDir || "cagstore") + "/" + e + "/" + n + "/" + o + "_" + r + ".jpg"
  ```

---

### Phase 7 —— 验证 MD5 算法

用 Python 复算，与抓包到的 `auth_key` 比对：

```python
import hashlib
path = "/cagstore/6228ab5acbfdb8048a47dd1d/13/0_0.jpg"
exp  = 1787062752
s = f"{path}-{exp}-0-0-ltfcdotnet"
print(hashlib.md5(s.encode()).hexdigest())
# 输出: 63409cfdc85493e8f823ef07ad259b4c  ✅ 与抓包完全一致
```

再实测下载（用自己生成的 auth_key）成功返回 200，算法确认无误。

---

### Phase 8 —— 还原瓦片网格（tile grid）

从 `createHDPTileSourceWithClipOffset` 中 `Math.floor(x / (2**(maxlevel-level)) / 512)`
确认**瓦片尺寸 = 512 像素**，级别 L 的网格为：

```
downscale = 2 ** (maxlevel - level)
nx = ceil(width  / (512 * downscale))
ny = ceil(height / (512 * downscale))
```

下载 level 18 的边缘瓦片实测尺寸验证：

| 瓦片 | 实际尺寸 | 说明 |
|------|----------|------|
| `18/0_0`   | 512 × 512 | 普通瓦片 |
| `18/20_0`  | 145 × 512 | 右边缘（10385 - 20×512 = 145） |
| `18/0_45`  | 512 × 506 | 下边缘（23546 - 45×512 = 506） |
| `18/20_45` | 145 × 506 | 右下角 |

与公式完全吻合 → 网格与拼接逻辑确认。

---

## 4. 关键结论汇总

### 4.1 完整下载算法

```
1. GET 作品页 HTML
2. 正则解析出每页:  name / resourceId / size(width,height) / maxlevel
3. 对每页, 在 level=maxlevel(18) 下:
     nx = ceil(width / 512),  ny = ceil(height / 512)
4. 对每个瓦片 (x, y):
     expires  = int(time.time()) + 300
     path     = /cagstore/{resourceId}/{level}/{x}_{y}.jpg
     auth_key = md5(f"{path}-{expires}-0-0-ltfcdotnet")
     url      = https://cag-ac.ltfc.net{path}?auth_key={expires}-0-0-{auth_key}
     GET url  → 瓦片 JPEG 字节
5. 拼接: 瓦片(x,y) 贴到画布 (x*512, y*512)，画布尺寸 = (nx-1)*512+边缘宽 × (ny-1)*512+边缘高
6. 保存为 JPEG
```

### 4.2 关键参数

| 项 | 值 |
|----|----|
| CDN 域名 | `https://cag-ac.ltfc.net` |
| 路径模板 | `/cagstore/{resourceId}/{level}/{x}_{y}.jpg` |
| 鉴权参数 | `auth_key={expires}-0-0-{md5}` |
| 鉴权密钥 | `ltfcdotnet` |
| 哈希算法 | MD5（`"{path}-{expires}-0-0-ltfcdotnet"`） |
| 瓦片尺寸 | 512 × 512 |
| 级别范围 | minlevel=12 ~ maxlevel=18（18 = 原图） |

### 4.3 分辨率对照

| level | 单页分辨率（约） | 瓦片数（约） |
|-------|------------------|--------------|
| 18（原图） | 10500 × 23500 | 966 |
| 17 | 5200 × 11800 | 242 |
| 16 | 2600 × 5900 | 72 |
| 15 | 1300 × 2900 | 18 |
| 14 | 650 × 1470 | 6 |

---

## 5. 附录：相关文件与代码位置

| 文件 | 作用 |
|------|------|
| `webpack-9df6862f0e97d6ab.js` | chunk ID → 文件名映射（`l.u`） |
| `page-e47ab76cebf705d8.js` | viewer 入口，动态 import 模块 47234 |
| `5610-6ec0727a379f5842.js` | **auth_key 生成算法**（模块 26549） |
| `8807-c3a75832eae93e10.js` | **瓦片路径拼接**（`createHDPTileSource`） |
| `9636-1234f39ab007dcc5.js` | **js-md5 库** |
| `2376-89c439eeb8c36ebe.js` | 图片 URL 追加 `image_process` 质量参数 |

### 5.1 核心 JS 反编译片段（chunk 5610，模块 26549）

```js
let i = /^(https*:\/\/[\w-.]*)(\/.+\.(jpeg|jpg|png))\?*(.*)$/;

function signUrl(e) {
  let m = e.match(i);
  if (!m) return e;
  let domain = m[1];            // https://cag-ac.ltfc.net
  let path   = m[2];            // /cagstore/{rid}/{level}/{x}_{y}.jpg
  let query  = m[4] || "";
  if (query) query += "&";
  let expires = Math.floor(Date.now() / 1000);
  let raw = `${path}-${expires}-0-0-ltfcdotnet`;
  let digest = md5(raw);
  return `${domain}${path}?${query}auth_key=${expires}-0-0-${digest}`;
}
```

### 5.2 核心 JS 反编译片段（chunk 8807，`createHDPTileSource`）

```js
// e = resourceId, i = 图片元信息(hdpic), n = level, o = 列x, r = 行y
function tilePath(e, i, n, o, r) {
  let dir = i.tilesDir || "cagstore";
  return "/" + dir + "/" + e + "/" + n + "/" + o + "_" + r + ".jpg";
}
// 拼接时再经 signUrl() 加上 auth_key
```

---

## 6. 逆向要点总结

1. **SSR 数据先行**：图片元数据（`resourceId`、`size`、`maxlevel`）直接藏在页面 HTML 里，
   无需猜接口。
2. **两套鉴权并存**：缩略图走 OSS 签名（服务端），高清瓦片走 `cag-ac` CDN 的 `auth_key`（客户端）。
3. **密钥藏在前端**：`auth_key` 的 MD5 密钥 `ltfcdotnet` 明文硬编码在 viewer 的 webpack chunk 中，
   通过「抓包拿到样例 + 反编译 chunk + MD5 复算」三步即可还原。
4. **瓦片网格公式**：512 像素瓦片，级别每 +1 分辨率翻倍，网格按 `ceil(尺寸 / 512 / 2^(maxlevel-level))` 计算。
